@helper HelperName(string name) {
    // your helper can contain code and/or markup, e.g.
    // var msg = "Hello " + name;
    // <p>@msg</p>
    // For more information, visit http://go.microsoft.com/fwlink/?LinkID=204658
}

@helper CustomStyleDropdownList(string name, IEnumerable<SelectListItem> itens, string labelFirstItem, string callBackJsFunction) { 
   
    <div class="myCustomDropdown">
        <ul>
            @foreach(SelectListItem item in itens)
            {
                <li>@item.Text </li>
            }
        </ul>
    </div>
    
}
